# Agent 工作规范

本文仅保存稳定、通用的 Agent 工作纪律。具体任务需求、实现细节、临时约束和验收内容必须写入 `REQUIREMENTS.md`、`PLAN.md` 或其他任务 Markdown，不得追加到本文。

## 多阶段 Subagent 串行执行与审查要求

1. 开始任务前，主 Agent 必须根据用户实际提出的需求数量、复杂度和依赖关系制定完整执行计划。计划大点数量不得少于可独立识别的主要需求点数量；复杂需求应继续拆分。每个大点必须明确目标、输入、执行范围、预期产出和验收标准。
2. 本规范属于长期 Agent 工作规范。实际修改仓库前，允许将本规范写入任务专用工作 Branch 中作用域适当的 `AGENTS.md`，后续 Agent 与 Subagent 均应遵守。
3. 具体任务需求不得继续追加到 `AGENTS.md`；任务要求应维护在 `REQUIREMENTS.md`、`PLAN.md` 或其他适当文件中。
4. 仓库原有 `AGENTS.md` 不得被覆盖；必须先读取并理解已有规则后合并。更深层目录的 `AGENTS.md` 在其作用域内优先。
5. 实施前必须读取并理解适用的 `AGENTS.md`、`CONTRIBUTING.md`、README、开发规范、CI 配置及与当前任务直接相关的文档。
6. 涉及仓库修改时，除非用户明确要求直接改稳定分支，应优先从任务指定基准创建独立工作 Branch，不直接污染 `main`、`master` 或其他稳定分支。
7. 创建工作 Branch 后必须记录基准 Branch 与基准 commit；最终 diff 只能包含本任务相关修改。
8. 允许在工作 Branch 中临时维护 `PLAN.md`、`REQUIREMENTS.md`、`SEARCH_SUMMARY.md`、`RESEARCH.md`、`DECISIONS.md`、`REVIEW.md`、`VALIDATION.md`、`TODO.md`、`NOTES.md` 等任务 Markdown；文件名按实际需要确定。此类任务记录默认由 `.gitignore` 排除，不作为长期项目文档。
9. 工作 Markdown 应少而清晰，避免无意义碎片；任务合并或交付前由 Agent 自动清理全部临时任务 Markdown，不能把过程记录遗留在仓库中。
10. `PLAN.md` 必须动态维护，阶段完成、返工、计划调整、需求变化或发现前置条件后应同步更新。
11. `REQUIREMENTS.md` 或等价文件保存当前任务具体需求；后续需求变化更新该文件，不更新 `AGENTS.md`。
12. 大量仓库搜索、外部检索或技术调查应维护 `SEARCH_SUMMARY.md`、`RESEARCH.md` 或等价记录，保存搜索范围、事实、证据、结论和未解决问题。
13. 若执行环境支持指定模型，整个任务主 Agent 与 Worker Subagent 均固定使用 Sol，reasoning effort 固定为 `high`，不得自动切换其他模型或 effort。
14. 若环境无法创建或调用指定 Sol + high Subagent，必须明确说明能力限制，不得用其他模型冒充或声称不存在的 Worker 已完成工作。
15. 必须严格按计划顺序推进；前一大点未完成并通过主 Agent 审查前，不得进入下一大点。显式依赖必须先满足前置条件。
16. 若环境具备 `exec` 与 Sol High Worker，每个大点均通过 `exec` 分发给当前唯一 Worker；主 Agent 负责规划、范围控制、审查和整合，Worker 负责当前大点实施。
17. 同一时刻只允许一名 Worker 工作；禁止并行修改、并行分析或并行执行会影响同一仓库、文件、测试环境或上下文状态的任务。
18. 即使阶段理论上可并行，也必须串行；前一 Worker 结束、成果完整接收并通过审查后才能进入下一阶段。
19. 每次只向 Worker 分发当前一个计划大点，流程必须是“执行当前阶段 → 主 Agent 审查 → 必要时返工 → 重新审查 → 通过 → 下一阶段”。
20. 分发阶段任务时必须提供原始要求、当前计划项、仓库状态、前序成果、约束、允许修改范围和验收标准等充分上下文。
21. Worker 处于 pending 或 running 状态时，即使等待时间较长，主 Agent 也不得向 Worker 发送催促、要求尽快返回、要求报告阻塞或其他改变其执行节奏的提示；只能使用等待机制获取其自然终态。
22. 对任何多阶段任务，计划中的 Phase 0/第 0 阶段只允许做最小基准状态确认：检查工作树无未提交业务修改、基准 commit/分支关系正确并执行必要的 `git diff --check`；不得额外审计基准架构、进行全仓搜索、重复运行测试或开展其他消耗 token 的基准审核，除非发现异常或用户明确要求。确认无异常后直接进入下一阶段。
23. 每个大点完成后主 Agent 必须亲自严格审查，不得直接相信 Worker 的完成或测试声明。
24. 涉及代码、配置或文件修改时，主 Agent 必须查看实际 diff，确认修改文件、增删内容、范围、意外改动、约束和验收目标。
25. 审查以用户原始需求、当前 `REQUIREMENTS.md`、当前计划项验收标准和适用 `AGENTS.md` 为依据，至少检查遗漏、范围扩大、逻辑、完整性、副作用、代码质量和测试覆盖。
26. 必须特别检查无必要兼容层、重复实现、死代码、临时 hack、无关格式化、大面积无意义重写、调试残留、错误注释、过期文档、无用依赖和计划外 API 变化。
27. 阶段未通过审查时，应由执行该阶段的同一 Sol High Worker 返工；不得为绕过问题随意更换 Worker。
28. 返工必须给出明确问题清单：未满足项、问题位置、不合格原因、目标状态和再次验收条件；禁止只说“重做”“继续优化”等模糊指令。
29. 返工后必须重新完整审查；仍不满足则继续由同一 Worker 修正，直到通过。
30. 只有当前阶段通过主 Agent 审查后才能进入下一大点；进入下一阶段前应记录已完成内容、关键决策和继续保持的约束。
31. 每阶段通过后同步更新 `PLAN.md` 或等价文件并记录必要验收结果；留待后续验证的问题必须显式记录。
32. 主 Agent 持续维护整体一致性；每完成若干阶段重新检查接口、依赖、文件、命名、测试、配置和架构是否与原始任务一致。
33. Worker 不得擅自扩大范围；发现计划外问题时记录并报告，是否纳入由主 Agent 决定。
34. 计划外问题若直接阻碍原任务，主 Agent 可调整计划，但必须记录原因、影响范围、新阶段和验收标准，并更新相关任务文档。
35. 代码、配置或文件修改必须以实际仓库状态为准；按需查看实际文件、diff、构建、测试、lint、格式、静态分析、安全检查等证据。
36. 在环境允许时，阶段 diff 应小而清晰，使目标与修改一一对应。
37. 允许为通过审查的阶段创建阶段性 commit；commit message 必须准确，不得把未通过工作描述为完成。
38. “测试通过”必须有实际执行证据；环境限制无法验证时必须标记“未验证”，不得虚构。
39. 测试必须覆盖实际影响范围，包括必要的现有单元、集成、构建、静态检查或回归验证。
40. 测试失败应尽量区分当前修改引入还是基准已有；声称历史问题时尽可能提供基线对照证据。
41. 前期方案存在根本错误时可由主 Agent 调整总体计划，并说明原因、影响阶段和剩余计划。
42. 重大计划调整后必须重新检查已完成阶段是否仍成立；失效则重新打开并验证。
43. 全部计划大点完成后必须审查工作 Branch 相对基准 Branch 的完整最终 diff，而非只看最后阶段。
44. 最终 diff 每项实质修改必须可追溯到用户需求、后续明确要求或已记录批准的计划调整；无法解释的修改视为异常。
45. 全部阶段完成后必须进行一次完整任务视角的最终总审查，重新验证完整性、正确性、一致性、回归风险、遗漏和测试充分性。
46. 最终审查必须处理工作 Markdown：无长期价值的纯临时记录在合并或交付前清理；有维护价值的文档才可保留在明确的文档目录中。
47. `AGENTS.md` 不属于普通临时任务文件。本规范可保留，但后续业务需求不得堆入其中。
48. 若通过 Pull Request 交付，PR 最终 diff 必须与最后一次总审查一致；总审查后若发生实质修改，相应 diff 和测试必须重新审查。
49. 最终输出必须明确列出原始需求对应计划项、各阶段状态和主要修改、关键审查与返工、实际测试、未验证内容、Branch/commit/PR 状态、工作 Markdown 去留，以及是否满足全部明确要求。
50. 若任何要求无法完成，必须明确对应需求、阻塞原因、已采取措施和当前真实状态，不得把部分完成、未验证或环境限制包装为全部完成。

## 核心执行纪律

- 计划规模不得机械固定为 10 个大点；必须覆盖全部独立需求，复杂需求继续拆分。
- 模型限制：环境支持时仅使用 Sol + high；不支持时必须公开能力限制。
- 仓库权限：允许从指定基准创建独立 Branch，并维护任务辅助 Markdown。
- 文档边界：稳定工作纪律写入 `AGENTS.md`；具体任务只写入任务文档。
- 核心流程：读取约束 → 写入 `AGENTS.md` → 建立临时 `REQUIREMENTS.md` → 制定并动态维护临时 `PLAN.md` → 串行执行阶段 → 实际 diff/测试审查 → 不合格返工 → 完成后检查完整 Branch diff → 最终回归与总审查 → 合并/交付前自动删除全部临时任务 Markdown → 交付。
- 禁止并发 Worker、禁止未经审查跨阶段推进、禁止冒充模型或 effort、禁止绕过实际 diff 与实际验证进行验收。

## 工具缺失与验证真实性

- 构建、测试、格式化、静态分析、集成或发布流程所需工具缺失时，Agent 必须先确认仓库声明的安装来源或系统包管理器，并在当前权限和任务授权范围内主动安装；不得仅因 `command not found` 跳过门禁。
- 工具安装完成后必须重新执行用户或仓库指定的原始命令。即使某个脚本看似等价，也不得以运行替代命令为由宣称原始门禁已经通过。
- 禁止捏造工具、替身、wrapper、命令输出、测试结果或环境状态；不得用 fake/stub binary 冒充真实依赖，也不得把未执行、失败或被跳过的验证写成 PASS。
- 若因权限、网络、平台或可信安装源缺失而确实无法安装，必须记录安装尝试、真实错误和已采取措施，并明确标记为“未验证”或“阻塞”；不得把环境限制包装成完成。

## Beta 版本纪律

- 当前项目处于 beta 阶段。代码、JSON schema、内部 API 或其他契约发生 breaking change 时，默认**直接修改当前 v1 契约**，不因为兼容性考虑自动升级为 `v2`。
- 除非用户以后明确要求，否则禁止为了保留旧行为新增兼容层、双 schema、迁移适配器或旧版本 fallback；旧设计可以直接删除或原地替换。
- `ecs.report/v1`、`ecs.compare/v1` 等当前版本标识应保持不变；版本号变化必须来自用户明确要求，而不是 Agent 自行依据 breaking change 推断。

## 临时任务 Markdown 生命周期

- `PLAN.md`、`REQUIREMENTS.md`、`REVIEW.md`、`SEARCH_SUMMARY.md`、`RESEARCH.md`、`DECISIONS.md`、`VALIDATION.md`、`TODO.md`、`NOTES.md` 和同类过程记录仅用于当前工作 Branch。
- 这些文件由 `.gitignore` 默认排除；需要长期维护的设计、使用或安全文档必须放在明确的正式文档位置，并经过单独审查。
- 每次工作完成并合并前，主 Agent 必须删除上述临时文件；合并后的工作区检查也必须确认没有残留。不得通过保留旧的空文件、归档副本或重复文件绕过清理要求。
