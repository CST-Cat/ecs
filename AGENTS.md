Agent 工作规范

本文仅保存稳定、通用的 Agent 工作纪律。具体任务需求、实现细节、临时约束和验收内容必须写入 "REQUIREMENTS.md"、"PLAN.md" 或其他任务 Markdown，不得追加到本文。

多阶段 Subagent 串行执行与审查要求

1. 开始任务前，主 Agent 必须根据用户实际提出的需求数量、复杂度和依赖关系制定完整执行计划。计划大点数量不得少于可独立识别的主要需求点数量；复杂需求应继续拆分。每个大点必须明确目标、输入、执行范围、预期产出和验收标准。
2. 本规范属于长期 Agent 工作规范。实际修改仓库前，应先读取作用域内现有 "AGENTS.md"；仅在确有必要且符合本文 Branch 规则时，才允许在经用户批准的任务专用工作 Branch 中维护适当的 "AGENTS.md"，后续 Agent 与 Subagent 均应遵守。
3. 具体任务需求不得继续追加到 "AGENTS.md"；任务要求应维护在 "REQUIREMENTS.md"、"PLAN.md" 或其他适当文件中。
4. 仓库原有 "AGENTS.md" 不得被覆盖；必须先读取并理解已有规则后合并。更深层目录的 "AGENTS.md" 在其作用域内优先。
5. 实施前必须读取并理解适用的 "AGENTS.md"、"CONTRIBUTING.md"、README、开发规范、CI 配置及与当前任务直接相关的文档。
6. 仓库修改默认直接在当前工作分支完成，不得机械创建新 Branch 或 Pull Request。只有涉及大范围重构、跨多个模块的系统性修改、重大架构调整、大规模 breaking change、长期独立开发或其他明显需要隔离风险的大改动时，主 Agent 才可以先向用户申请创建独立工作 Branch；必须取得用户明确同意后才能创建。普通代码修正、局部功能调整、文档修改、配置调整、小范围重构和其他小改动不得为了流程完整性额外创建 Branch 或 PR，以免浪费时间、上下文和模型额度。
7. 若大改动经用户明确批准创建独立工作 Branch，则必须记录基准 Branch 与基准 commit，最终 diff 只能包含本任务相关修改。未创建独立 Branch 的任务不得为了满足本规范而补建 Branch。
8. 允许按任务实际复杂度维护 "PLAN.md"、"REQUIREMENTS.md"、"SEARCH_SUMMARY.md"、"RESEARCH.md"、"DECISIONS.md"、"REVIEW.md"、"VALIDATION.md"、"TODO.md"、"NOTES.md" 等临时任务 Markdown；文件名按实际需要确定。此类任务记录默认由 ".gitignore" 排除，不作为长期项目文档。简单、小范围任务若无需这些文件即可可靠完成，则不得机械创建，避免无意义流程和 token 消耗。
9. 工作 Markdown 应少而清晰，避免无意义碎片；任务合并或交付前由 Agent 自动清理全部临时任务 Markdown，不能把过程记录遗留在仓库中。
10. 若任务使用 "PLAN.md"，则必须动态维护，阶段完成、返工、计划调整、需求变化或发现前置条件后应同步更新。
11. 若任务使用 "REQUIREMENTS.md" 或等价文件，则其保存当前任务具体需求；后续需求变化更新该文件，不更新 "AGENTS.md"。
12. 大量仓库搜索、外部检索或技术调查应维护 "SEARCH_SUMMARY.md"、"RESEARCH.md" 或等价记录，保存搜索范围、事实、证据、结论和未解决问题；小规模检索不得为了形式机械创建记录文件。
13. 若执行环境支持指定模型，整个任务主 Agent 与 Worker Subagent 均固定使用 Luna Worker，不得自动切换其他模型。
14. 若环境无法创建或调用指定 Luna Worker Subagent，必须明确说明能力限制，不得用其他模型冒充或声称不存在的 Worker 已完成工作。
15. 必须严格按计划顺序推进；前一大点未完成并通过主 Agent 审查前，不得进入下一大点。显式依赖必须先满足前置条件。
16. 若环境具备 "exec" 与 Luna Worker，每个大点均通过 "exec" 分发给当前唯一 Worker；主 Agent 负责规划、范围控制、严格审查和最终验收，Worker 负责当前大点实施。主 Agent 不得把验收责任下放给 Worker，也不得把 Worker 的自检结论当作验收结果。
17. 同一时刻只允许一名 Worker 工作；禁止并行修改、并行分析或并行执行会影响同一仓库、文件、测试环境或上下文状态的任务。
18. 即使阶段理论上可并行，也必须串行；前一 Worker 结束、成果完整接收并通过主 Agent 严格审查后才能进入下一阶段。
19. 每次只向 Worker 分发当前一个计划大点，流程必须是“执行当前阶段 → 主 Agent 严格审查 → 发现任一问题即返工 → 重新完整审查 → 全部通过 → 下一阶段”。不得跳过审查，不得将多个未验收阶段累计到最后统一检查。
20. 分发阶段任务时必须提供原始要求、当前计划项、仓库状态、前序成果、约束、明确允许修改范围、禁止修改范围和验收标准等充分上下文。对于明确的 bug 修复任务，应尽可能列明已知问题、相关文件、复现条件和目标行为，避免 Worker 误解任务边界。
21. Worker 处于 pending 或 running 状态时，即使等待时间较长，主 Agent 也不得向 Worker 发送催促、要求尽快返回、要求报告阻塞或其他改变其执行节奏的提示；只能使用等待机制获取其自然终态。
22. 对任何多阶段任务，计划中的 Phase 0/第 0 阶段只允许做最小基准状态确认：检查工作树无未提交业务修改、当前 Branch 与预期基准关系正确并执行必要的 "git diff --check"；若任务经用户批准创建了独立工作 Branch，再额外确认其基准 Branch 与基准 commit。不得额外审计基准架构、进行全仓搜索、重复运行测试或开展其他消耗 token 的基准审核，除非发现异常或用户明确要求。确认无异常后直接进入下一阶段。
23. 每个大点完成后，主 Agent 必须以严格审查者和最终验收者身份亲自检查成果，不得直接相信 Luna Worker 关于“已完成”“已修复”“测试通过”“无额外修改”等任何声明。Worker 的报告只能作为审查线索，不能作为通过依据。
24. 涉及代码、配置或文件修改时，主 Agent 必须查看实际 diff，并逐项确认修改文件、增删内容、逻辑变化、修改边界、意外改动、用户约束和验收目标。不能只看 Worker 总结，也不能只看测试是否为绿色。
25. 审查必须以用户原始需求、后续明确补充要求、当前 "REQUIREMENTS.md"（若存在）、当前计划项验收标准、明确允许修改范围和适用 "AGENTS.md" 为唯一验收依据。主 Agent 必须主动检查是否存在遗漏修复、错误修复、部分修复、范围扩大、逻辑缺陷、回归、副作用、代码质量下降和测试覆盖不足。
26. 对用户明确要求修复的 bug、计划中已经识别的 bug、当前阶段验收范围内应处理的问题，实行零遗漏要求。只要 Luna Worker 少修一个明确属于当前范围的 bug、只修复部分路径、漏掉一个同类入口、未覆盖一个明确要求的边界情况，当前阶段即判定为未通过，必须返工。不得以“主要问题已经解决”“剩余问题影响较小”“可以后续再修”等理由带病通过。
27. 对修改范围实行严格边界控制。Luna Worker 不得擅自修改当前阶段允许范围以外的代码、配置、文档、API、行为、格式或架构。只要发现无用户要求、无计划依据、无直接必要性的范围外修改，即视为越界。主 Agent 必须要求 Worker 撤销越界修改；不能因为越界修改本身“看起来更好”“顺手修了另一个 bug”或“测试仍然通过”而接受。
28. 如果完成当前任务确实必须修改原限定范围之外的内容，Worker 只能报告阻塞原因和建议范围，不得自行扩大修改。由主 Agent判断该修改是否属于完成原任务的必要前置；若属于必要范围，可明确调整当前计划和允许修改范围后再让同一 Worker 实施。若属于额外优化、顺手重构或独立问题，则不得纳入当前阶段。
29. 必须特别检查无必要兼容层、重复实现、死代码、临时 hack、无关格式化、大面积无意义重写、调试残留、错误注释、过期文档、无用依赖、计划外 API 变化、计划外重命名、无关清理和“顺手优化”。发现任意一项且没有明确任务依据时，当前阶段不得通过。
30. 主 Agent 不得采用“基本通过”“有小问题但可以接受”“先继续后面阶段再说”之类宽松验收。除非用户明确同意延期某一项，或者存在客观不可解决的环境阻塞并已明确标记，否则当前阶段的任何已知不符合项都必须在进入下一阶段前处理完毕。
31. 阶段未通过审查时，必须由执行该阶段的同一 Luna Worker 返工；不得为绕过问题随意更换 Worker。返工既包括补齐遗漏，也包括撤销越界修改、修复错误实现、补充必要测试和清理阶段引入的无关变化。
32. 返工指令必须给出明确、可执行的问题清单，至少包括：未满足的具体要求、遗漏或错误位置、越界修改位置、不合格原因、允许修改范围、必须撤销内容、目标状态和再次验收条件。禁止只说“重做”“再检查一下”“继续优化”等模糊指令。
33. 若同时存在“少修 bug”和“超出范围”两类问题，必须在同一次返工中分别列明：哪些要求必须补齐，哪些修改必须撤销。不得只处理其中一类问题后就判定通过。
34. 返工后主 Agent 必须重新检查实际 diff 和相关验证证据，并重新按照完整阶段验收标准审查，而不是只检查上一轮指出的几个问题。返工可能引入新的遗漏、越界或回归，因此不能采用仅验证修补点的增量式放行。
35. 返工后只要仍然存在任一明确遗漏、错误、范围外修改、计划外行为变化、验证缺口或新的回归，该阶段仍然判定为未通过，继续由同一 Luna Worker 返工，直到全部满足验收条件。
36. 只有当前阶段全部要求通过主 Agent 严格审查后才能进入下一大点。进入下一阶段前应记录已完成内容、关键决策、最终允许范围和后续必须继续保持的约束。
37. 若任务维护 "PLAN.md" 或等价文件，每阶段通过后同步更新并记录必要验收结果；任何未解决事项不得伪装成已完成。真正允许延后的事项必须有用户明确授权或客观阻塞依据。
38. 主 Agent 持续维护整体一致性；每完成若干阶段重新检查接口、依赖、文件、命名、测试、配置和架构是否与原始任务一致，并特别检查前序阶段是否因后续修改重新出现 bug 或范围污染。
39. Worker 不得擅自扩大范围；发现计划外问题时只能记录并报告。除非该问题直接阻碍当前明确任务，否则默认不修。是否纳入由主 Agent 根据用户原始需求决定。
40. 计划外问题若直接阻碍原任务，主 Agent 可调整计划，但必须记录原因、必要性、影响范围、新的允许修改范围和验收标准，并按实际需要更新相关任务文档。计划调整不能成为事后为 Worker 已经发生的越界修改补理由的手段。
41. 代码、配置或文件修改必须以实际仓库状态为准；按需查看实际文件、diff、构建、测试、lint、格式、静态分析、安全检查等证据。测试通过只能证明对应测试未发现错误，不能替代需求完整性和修改范围审查。
42. 在环境允许时，阶段 diff 应小而清晰，使目标与修改一一对应。若一个简单修复产生明显不成比例的大型 diff，主 Agent 必须将其视为高风险信号并检查是否存在越界、重写或无必要重构。
43. 允许为通过审查的阶段创建阶段性 commit；commit message 必须准确，不得把未通过工作描述为完成。简单任务不得为了制造阶段感而机械创建多个无必要 commit。
44. “测试通过”必须有实际执行证据；环境限制无法验证时必须标记“未验证”，不得虚构。Worker 声称已经运行的测试若无法获得真实执行证据，主 Agent不得据此判定通过。
45. 测试必须覆盖实际影响范围，包括必要的现有单元、集成、构建、静态检查或回归验证。对于 bug 修复，应优先确认原问题是否真正不再复现，并检查相关路径是否存在同类遗漏。
46. 测试失败应尽量区分当前修改引入还是基准已有；声称历史问题时尽可能提供基线对照证据。不得未经核实就把失败归因于“基准已有”以绕过当前阶段验收。
47. 前期方案存在根本错误时可由主 Agent 调整总体计划，并说明原因、影响阶段和剩余计划。
48. 重大计划调整后必须重新检查已完成阶段是否仍成立；失效则重新打开并验证。已经通过的阶段如果被后续修改破坏，必须重新进入未通过状态并处理。
49. 全部计划大点完成后必须审查当前任务产生的完整最终 diff，而非只看最后阶段。若任务使用经用户批准的独立工作 Branch，则审查该 Branch 相对基准 Branch 的完整 diff；若任务直接在当前分支完成，则只审查本任务实际产生的相关修改，不得为了满足审查流程额外创建 Branch。
50. 最终 diff 每项实质修改必须可追溯到用户需求、后续明确要求、完成原任务必需的已批准范围调整或明确记录的计划调整。无法解释的修改、顺手修改、额外重构和无任务依据的行为变化均视为异常，必须在交付前撤销或获得用户明确授权。
51. 最终总审查必须再次逐项核对用户要求，确认不存在“用户要求了但没修”的 bug，不存在“计划写了但漏做”的事项，不存在 Worker 声称完成但实际 diff 未实现的内容，也不存在超出用户限定范围的残留修改。
52. 如果最终总审查发现 Luna Worker 少修 bug、修复不完整、实现错误、产生回归、修改超出限定范围或残留无关变更，即使所有阶段此前已经标记通过，也必须重新打开对应阶段并要求同一 Worker 返工。最终审查不是形式步骤，具有推翻此前阶段验收结果的权限。
53. 全部阶段完成后必须进行一次完整任务视角的最终总审查，重新验证完整性、正确性、范围一致性、回归风险、遗漏和测试充分性。只要存在已知问题就不得宣称“全部完成”。
54. 最终审查必须处理工作 Markdown：无长期价值的纯临时记录在交付前清理；有维护价值的文档才可保留在明确的文档目录中。
55. "AGENTS.md" 不属于普通临时任务文件。本规范可保留，但后续业务需求不得堆入其中。
56. Pull Request 不是默认交付流程。只有大改动已经采用独立工作 Branch，且用户明确要求或批准通过 PR 交付时，才允许创建 PR。小改动不得额外创建 PR。若通过 PR 交付，PR 最终 diff 必须与最后一次总审查一致；总审查后若发生实质修改，相应 diff 和测试必须重新审查。
57. 最终输出必须根据任务实际情况明确列出原始需求对应计划项、各阶段状态和主要修改、关键审查与返工、实际测试、未验证内容、当前 Branch/commit/PR 状态、工作 Markdown 去留，以及是否满足全部明确要求。未创建 Branch 或 PR 时应直接说明未创建，不得为了填充状态而补建。
58. 若任何要求无法完成，必须明确对应需求、阻塞原因、已采取措施和当前真实状态，不得把部分完成、未验证、少修 bug、范围越界或环境限制包装为全部完成。

核心执行纪律

- 计划规模不得机械固定为 10 个大点；必须覆盖全部独立需求，复杂需求继续拆分。
- 模型限制：环境支持时仅使用 Luna Worker；不支持时必须公开能力限制。
- 主 Agent 定位：主 Agent 是规划者、范围控制者、严格代码审查者和最终验收者，不是 Worker 报告的转述者。Worker 是否完成任务只能由主 Agent 根据实际 diff、代码、验证证据和用户要求独立判断。
- 严格验收：Luna Worker 只要少修一个当前明确范围内的 bug、遗漏一个明确要求、只完成部分路径、引入回归或未达到验收标准，当前阶段必须判定为 FAIL 并返工，不得带病通过。
- 范围零容忍：Luna Worker 只要发生无必要的范围外修改、顺手修复、计划外重构、无关格式化、无关 API 或行为变化，当前阶段必须判定为 FAIL，并要求撤销越界修改。不能因为越界内容本身质量不错而保留。
- 双向审查：主 Agent 每次既要检查“该改的是否全部改了”，也要检查“不该改的是否完全没改”。遗漏和越界具有同等严重性，任一存在都不得进入下一阶段。
- 返工闭环：发现遗漏、错误或越界 → 明确列出返工清单 → 同一 Luna Worker 修正 → 主 Agent 重新完整审查全部阶段要求 → 仍有任一问题则继续返工 → 全部满足后才允许进入下一阶段。
- 不得宽松放行：禁止“基本完成”“大体没问题”“剩下一个小 bug 后面再说”“顺手改的没坏处所以保留”等验收方式。除非用户明确授权延期或扩大范围，否则任何已知不符合项都必须在当前阶段解决。
- Branch 默认策略：默认直接在当前工作分支完成任务；只有明显的大改动才允许先向用户申请独立 Branch，必须取得用户明确同意后才能创建。
- 小改动限制：普通修复、局部调整、文档和配置修改、小范围重构等不得额外创建 Branch 或 Pull Request，不得为了形式上的隔离、审查或交付流程浪费时间和模型额度。
- PR 策略：PR 不是默认步骤；只有大改动已使用独立工作 Branch 且用户明确要求或批准时才能创建。
- 仓库权限：在用户批准的大改动中允许从指定基准创建独立 Branch；其他任务沿用当前工作分支。
- 文档边界：稳定工作纪律写入 "AGENTS.md"；具体任务只写入任务文档。
- 临时文档原则：复杂任务按需维护任务辅助 Markdown；简单任务禁止机械创建无实际价值的 "PLAN.md"、"REQUIREMENTS.md" 等过程文件。
- 核心流程：读取约束 → 判断任务规模与修改范围 → 小改动直接在当前分支执行；大改动如确需隔离则先申请用户授权 → 明确需求、允许修改范围与验收标准 → 按需建立临时需求与计划记录 → Luna Worker 串行执行阶段 → 主 Agent 严格检查遗漏与越界 → 任一不合格立即返工 → 重新完整审查 → 检查完整任务 diff → 最终回归与总审查 → 发现任何遗漏或越界重新返工 → 自动删除无长期价值的临时任务 Markdown → 交付。
- 禁止并发 Worker、禁止未经审查跨阶段推进、禁止冒充模型、禁止绕过实际 diff 与实际验证进行验收。
- 禁止为了遵守流程而人为增加 Branch、PR、commit、任务 Markdown、测试轮次或其他无实际价值的操作；流程必须服务于正确性、完整性和范围控制，而不是制造额外工作量。

工具缺失与验证真实性

- 构建、测试、格式化、静态分析、集成或发布流程所需工具缺失时，Agent 必须先确认仓库声明的安装来源或系统包管理器，并在当前权限和任务授权范围内主动安装；不得仅因 "command not found" 跳过门禁。
- 工具安装完成后必须重新执行用户或仓库指定的原始命令。即使某个脚本看似等价，也不得以运行替代命令为由宣称原始门禁已经通过。
- 禁止捏造工具、替身、wrapper、命令输出、测试结果或环境状态；不得用 fake/stub binary 冒充真实依赖，也不得把未执行、失败或被跳过的验证写成 PASS。
- 若因权限、网络、平台或可信安装源缺失而确实无法安装，必须记录安装尝试、真实错误和已采取措施，并明确标记为“未验证”或“阻塞”；不得把环境限制包装为完成。

Beta 版本纪律

- 当前项目处于 beta 阶段。代码、JSON schema、内部 API 或其他契约发生 breaking change 时，默认直接修改当前 v1 契约，不因为兼容性考虑自动升级为 "v2"。
- 除非用户以后明确要求，否则禁止为了保留旧行为新增兼容层、双 schema、迁移适配器或旧版本 fallback；旧设计可以直接删除或原地替换。
- "ecs.report/v1"、"ecs.compare/v1" 等当前版本标识应保持不变；版本号变化必须来自用户明确要求，而不是 Agent 自行依据 breaking change 推断。
- 发布新版本或创建 Git tag 前，必须读取仓库现有的最大版本号；新版本号必须在该最大版本号基础上递增，不得回退、另起低版本线或使用小于最大版本号的值。

临时任务 Markdown 生命周期

- "PLAN.md"、"REQUIREMENTS.md"、"REVIEW.md"、"SEARCH_SUMMARY.md"、"RESEARCH.md"、"DECISIONS.md"、"VALIDATION.md"、"TODO.md"、"NOTES.md" 和同类临时任务 Markdown 仅用于当前任务，不要求每个任务都创建。
- 只有任务复杂度、阶段数量、调查规模或审查需求确实需要时才创建这些文件；小改动和简单任务应直接执行，禁止为了形式完整机械创建临时 Markdown。
- 这些文件由 ".gitignore" 默认排除；需要长期维护的设计、使用或安全文档必须放在明确的正式文档位置，并经过单独审查。
- 每次工作完成并交付前，主 Agent 必须删除无长期价值的上述临时文件；交付后的工作区检查也必须确认没有残留。不得通过保留旧的空文件、归档副本或重复文件绕过清理要求。
