# Agent 工作规范

本文只保存稳定、通用的 Agent 工作纪律。用户的具体任务、实现细节、阶段要求和临时约束写入 `REQUIREMENTS.md`、`PLAN.md` 或其他适当的任务 Markdown，不得追加到本文。本文不得被覆盖；只有确有必要、先读取并理解现有规则且符合经用户批准的任务分支条件时，才可维护适当的 `AGENTS.md`，新增稳定纪律须与现有内容合并。更深层目录的 `AGENTS.md` 在其作用域内优先。

## 范围、计划与工作区

- 修改前读取适用的 `AGENTS.md`、`CONTRIBUTING.md`、README、开发规范、CI 配置和当前任务直接相关的文档。简单任务不机械创建过程文档；确需维护时，只记录清晰、必要的计划、需求、搜索、研究、决策、验证或待办内容。大量搜索或外部调查应记录范围、事实、证据、结论和未决事项。
- 开始任务前，主 Agent 根据用户实际需求制定完整计划。计划点覆盖每个可独立识别的主要需求，复杂需求继续拆分；每点写明目标、输入、范围、产出和验收标准。不得把计划数量机械固定为十个。Phase 0 只做最小基线确认：工作树无未提交业务修改、分支和基准关系正确、必要的 `git diff --check`；获准使用隔离分支时再确认基准分支和 commit。除非发现异常或用户明确要求，不得额外审计、搜索或重复测试；基线无异常后直接进入下一阶段。
- 默认直接在当前分支工作，不机械创建分支或 PR。只有大范围重构、跨模块系统性修改、重大 breaking change、长期独立开发等明显需要隔离的工作，且用户明确同意后才可创建工作分支；须记录基准分支和 commit，最终 diff 只能包含任务修改。未创建分支不得为了流程补建分支。PR 只有已获准隔离且用户明确要求或批准时才可使用；其最终 diff 必须与最近一次完整审查一致，实质修改后重新审查。
- 若使用计划或需求文件，阶段完成、返工、计划调整、需求变化和前置条件变化时同步维护。任务文档应少而清晰、默认被 `.gitignore` 排除；长期有维护价值的内容才进入正式文档目录。

## 串行执行与严格审查

- 环境支持时，主 Agent 和 Worker 固定使用 Luna Worker；不可用时必须如实说明，不能用其他模型冒充。若具备 `exec` 与 Luna Worker，每个计划点通过 `exec` 分发给当前唯一 Worker 执行；不得并行 Worker，或并行分析、执行、修改会影响同一工作区、文件、测试环境或上下文的工作；也不得在 Worker pending/running 时催促、改变节奏或要求报告阻塞，只能用等待机制取得自然终态。
- 一次只分发当前一个阶段；即使阶段理论上可并行也必须串行。分发内容须包含用户要求、计划点、仓库状态、前序成果、允许和禁止范围、验收标准以及足够的调用链和复现信息。前一阶段必须经主 Agent 完整审查并通过后才能进入下一阶段；阶段之间的依赖必须先满足。
- 主 Agent 是规划者、范围控制者和最终验收者，Worker 的“完成”或“测试通过”只作线索。每个阶段都要亲自看实际 diff，依据原始及后续用户要求、当前 `REQUIREMENTS.md`（若有）、当前阶段标准、允许范围和适用规则，逐项检查逻辑、回归、意外修改、遗漏和越界；同时检查“该改的是否全部改了”和“禁止改的是否完全没改”。
- 用户明确要求、计划已识别或当前阶段应处理的问题实行零遗漏。任何少修、部分修复、错误实现、已知回归、范围外修改、验证缺口或质量下降都不得以“基本完成”放行，除非用户明确授权延期，或存在客观上无法解决且已明确标记的环境阻塞。发现问题时由同一 Worker 返工；返工指令列出未满足项、遗漏或错误位置、越界修改、必须撤销的内容、不合格原因、允许范围、目标状态和再次验收条件。返工后重新完整审查，不只检查补丁点；上一阶段若被后续修改破坏，重新打开并验证。不得事后为已经发生的越界修改补理由。
- 如果完成当前任务确实需要超出原限定范围，Worker 只能报告阻塞原因和建议范围，不得自行扩大；由主 Agent 在 Worker 继续前判断是否为必要前置，并记录原因、必要性、影响、调整后的范围和验收标准，额外优化或独立问题不纳入。计划外问题默认只记录并报告，除非直接阻碍当前任务。遗漏与越界须在同一次返工中分别列明并处理。
- 主 Agent 在阶段间和阶段后持续检查接口、依赖、文件、命名、测试、配置和架构一致性。前期方案若根本错误，可说明原因、影响和剩余计划后调整；重大计划调整后重新检查所有已接受阶段，计划调整不得成为事后合理化。每个阶段通过后记录完成内容、关键决策、最终允许范围、后续约束和实际验收证据；未解决事项不得标记为完成。不得跳过根审查、把多个未验收阶段积累到最后，或在存在已知不符合项时继续推进。

## 验证、工具与代码质量

- 所有判断以实际仓库状态为准。按范围检查源码、diff、构建、测试、格式、静态分析、安全检查和脚本门禁；测试通过只证明对应检查未发现问题，不能代替需求和边界审查。测试应覆盖实际影响范围；修 bug 时优先确认原问题不再复现并检查同类入口。失败时尽量用基线或其他证据区分当前修改与既有问题，不能未经核实归因于基线。
- 验收必须有真实执行证据。缺工具时先确认仓库声明的安装来源或系统包管理器，并在当前权限和任务授权内主动安装；安装后重新执行用户或仓库要求的原始命令，不得以替代命令宣称通过。不可安装时记录尝试、真实错误和已采取措施，明确标记未验证或阻塞。禁止捏造工具、替身、wrapper、输出、测试或环境；不得用 fake/stub binary 冒充真实依赖、把跳过写成通过，或为了形式增加无实际价值的检查、框架、分支、测试轮次或流程。
- 代码和脚本改动保持最小、清晰、可追溯。特别检查死代码、无用兼容层、重复实现、临时 hack、无关格式化、调试残留、错误注释、过期文档、无用依赖、计划外 API/行为变化、重命名、清理和架构重写；简单任务产生明显不成比例的 diff 时提高审查强度。
- Go 格式只能以仓库的 `scripts/gofmt.sh` 为 canonical formatter；不得用 Agent PATH 中的裸 `gofmt` 判断仓库格式。任务范围外文件只有在 canonical formatter 明确报告失败且主 Agent 批准范围调整后，才可进行纯 formatter 修复；不得借此修改逻辑。

## 交付、版本与任务生命周期

- beta 项目直接维护当前 v1 契约：除非用户明确要求，禁止为 breaking change 新增旧行为兼容层、双 schema、迁移适配器或 legacy fallback；`ecs.report/v1`、`ecs.compare/v1` 等标识保持不变。发布版本或 Git tag 前读取仓库现有最大版本号，只在其基础上递增，不回退或另起更低版本线。
- 可以为已通过审查的阶段创建准确的阶段 commit，但不得机械制造 commit；未通过的工作不得用完成式描述。
- 全部阶段结束时，主 Agent 重新审查本任务产生的完整最终 diff（当前分支工作则审查任务实际修改，隔离分支则审查相对基准的完整 diff），逐项核对原始及后续用户要求、当前 `REQUIREMENTS.md`（若有）、计划、阶段标准、允许范围、适用规则、遗漏、回归、范围和验证；每项实质修改都必须能追溯到用户要求、计划、必要的范围调整或明确记录的前置条件。若发现任何遗漏、错误、范围外修改或验证缺口，必须让同一 Worker 返工，不能因早先阶段已通过而保留；无依据的修改须在交付前撤销或取得用户明确授权。交付前或任务合并前清理无长期价值的临时 Markdown，并确认没有残留；正式保留的耐久文档须经过适当审查，`AGENTS.md` 不属于临时文件。最终报告必须把原始需求对应到阶段状态，说明是否全部满足，并列出每阶段状态、主要修改、关键审查与返工、实际命令及结果、未验证内容、当前分支/commit/PR 状态（未创建时明确说明）、临时文档去留；任何无法完成的要求都须说明原因和已采取措施，阻塞或不确定性也如实列出，不得包装成全部完成。
