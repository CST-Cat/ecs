# 执行计划

基准：`main@6e85039301fc817e8c4a290ffb9b111fc37e55a8`

工作 Branch：`codex/architecture-machine-facts-cleanup`

状态：`TODO` / `IN PROGRESS` / `DONE` / `BLOCKED` / `REOPENED`。

> 本计划在当前工作区严格串行执行；本轮目标是完成修复并更新现有 Draft PR #6。远端分支必须先核对后以 fast-forward 同步，验证证据集中记录在 `VALIDATION.md`，未执行的外部集成项明确列在 `REVIEW.md`。

## 本轮复审状态（2026-08-23）

本文件前面的 0–25 阶段是截至历史提交 `a938385` 的执行记录，不代表本轮复审后的当前通过状态。复审目标为
`c6a59b37d2e61708bd3487dc13a8881fd718efaf`，发现 4 个 P1、3 个 P2 和 1 个 P3；相关历史 DONE 结论已由下列 remediation 阶段重新打开。任何历史“已完成”描述只能作为背景，不能替代本轮验收。

当前任务：更新 Draft PR #6；PR 现状保持 Draft，远端 target/base 当前分别为
`c6a59b37d2e61708bd3487dc13a8881fd718efaf` / `6e85039301fc817e8c4a290ffb9b111fc37e55a8`。
本地阶段 15 审计候选为 `c6ee5ba17ca6d902f88d549b87335c67e622db7f`；它与远端 target 是不同状态，且阶段 15 尚未完成，不能称为最终交付 SHA。阶段 13/14 记录中的 `2ac564a`、`98f568a` 仅是各自阶段的历史候选。

本轮只允许一名 Luna Worker 按顺序执行下列阶段。每阶段必须由主 Agent 独立审查实际 diff、文件范围和测试结果；不合格时由同一 Worker 返工并重新完整审查，未通过不得推进。

## Remediation 阶段记录

1. **基线与任务记录 — DONE**。目标：同步精确 target 并记录本轮范围。输入：远端 base/target、AGENTS/README/CONTRIBUTING/CI 和任务文档。产出：fast-forward、REQUIREMENTS.md addendum、当前 PLAN 记录。验收：主 Agent 已复核 HEAD=`c6a59b37d2e61708bd3487dc13a8881fd718efaf`、merge-base=`6e85039301fc817e8c4a290ffb9b111fc37e55a8`，diff 仅含 `REQUIREMENTS.md`/`PLAN.md`，`git diff --check` 通过；不修改 AGENTS/生产代码。
2. **Message 格式契约 — DONE**。目标：消除 `%!d(string=...)`。输入：Message 模型、生产模板和双语 catalog。产出：兼容模板及全量参数契约测试。验收：受影响模块中英文渲染无 `%!`，所有生产带参 Message 调用与模板参数数目一致。首次主审指出测试把全部 `probe.*` presentation key 当作 fmt 模板、且静默跳过动态 key；同一 Worker 已改为按生产 `NewMessage` AST 调用审计、登记 NAT/Privacy 动态 key 契约并禁止绕过 `NewMessage` 的生产 `Message{}`，复审通过。主 Agent 独立证据：目标包及 `go test -count=1 ./...` 通过，英文 local system 动态运行输出正常且无 `%!`。
3. **retry/interference 结构化模型 — DONE**。目标：canonical JSON 不写展示散文。输入：RetryInfo、Interference、TextBlock 和 pressure 后处理。产出：`Result.Interference`、Message 化的 retry/reasons、`TextBlock.Attempt`、稳定 pressure key 及双语 catalog；删除 retry/interference 旧中文标题、Fields、Tables、Notes 注入。验收：主 Agent 已独立复核接口变更、旧展示注入删除、Message/measurement 深拷贝与脱敏、Attempt 标记及实际 canonical JSON；目标包 `go test -count=1 ./internal/i18n ./internal/model ./internal/probe ./internal/report ./internal/runner` 与全仓 `go test -count=1 ./...` 通过，`git diff --check` 通过。
4. **retry/interference renderer — DONE**。目标：展示边界本地化。输入：阶段 3 模型、terminal/Markdown/HTML renderer。产出：统一临时展示视图及三格式渲染契约测试。首次主审指出测试缺少英文全量 Han 字符检查、且 stable key 泄漏断言遗漏 `report.interference.*`/`report.attempt.*`；同一 Worker 已补充逐 rune 英文检查和完整阶段 key-prefix 集合，复审通过。验收：Text/Markdown/HTML 三格式双语重渲染显示 interference/retry 全部结构事实、Attempt 前缀与 raw evidence；compact Text 保留 retry 判定；HTML 转义安全；渲染前后 canonical JSON 字节不变。主 Agent 独立证据：目标包及 `go test -count=1 ./...` 通过、`git diff --check` 通过，并完成真实英文 CPU local 动态验证，interference 已英文显示且无旧中文或 `%!`。
5. **plan/run exposure/reveal — DONE**。目标：向导最终选择完整传入 run。输入：execution plan、wizard 和 run.sh。产出：`ecs.plan/v1` 必需 `reveal` 字段、严格 exposure/reveal 解析和所有普通 run 分支的权威参数追加。首次主审指出四类验收覆盖缺口：Go 仅覆盖 local、shell 仅覆盖 local/public、缺少重复字段 fixture、ordinary argv 缺少精确长度及末尾参数断言；同一 Worker 已补充并经复审通过。验收：8 组 Go exposure×reveal 组合逐项确认 v1、enum、JSON boolean 与无本地化 prose；shell 已覆盖 4 个合法 exposure、两种 reveal、冲突 last-wins、缺失/非法/重复 exposure/reveal 的中英文 fail-closed，且最终 run 不执行；submit 路径未改变。主 Agent 独立证据：`gofmt`、`sh -n`、app/config 测试、`bash scripts/run_test.sh`、`go test -count=1 ./...` 与 `git diff --check` 均通过。
6. **network 直接机器语义 — DONE**。目标：删除 network 展示文案逆向识别并修复 DB-IP。输入：source/provider/score/status producer。产出：producer 直接写 stable ID/enum/i18n key；删除 `networkSemanticProbe`、`stabilizeNetworkResult`、`networkSourceID` 等 reverse bridge；DB-IP 使用 `dbip`；补齐 ip-api/ip.sb source disclosure、origin/finding 有限状态和 report Message/source-name 展示契约。首次主审指出 warning 未由 failed/partial 机器状态驱动、ip-api/ip.sb 来源披露缺失、`qualityFinding.Name`/`qualitySourceLabels` 双重 identity、origin/status 测试不足、DB-IP/来源行断言不足以及 report stable arg/name 契约缺口；同一 Worker 第一轮返工后，第二次主审又指出双栈 summary 消息无分隔导致粘连、failed/partial note 双栈重复、`failedSourceNames`/`partialSourceNames` 命名误导，随后同一 Worker 第二轮返工。第三次完整主审通过：direct ID 语义、`probe.network.score_band.dbip`、无 network reverse bridge、双语 renderer、双栈 summary/note 聚合及 canonical JSON 不变均已独立核验。验收证据：所有 network-owned source/field/table/status/score/measurement key 在中英文注册；failed、partial、disabled、ok 和 origin 分支均由机器状态测试覆盖；实际命令 `go test -count=1 ./internal/i18n ./internal/model ./internal/probe ./internal/report ./internal/runner`、`go test -count=1 ./...`、`go test -race -count=1 ./internal/probe ./internal/report`、`git diff --check` 均通过。
7. **media 直接机器语义 — DONE**。目标：删除 media 文案逆向识别。输入：媒体 verdict/evidence/strength 和原始错误。产出：producer 直接生成有限 verdict/evidence/strength、稳定字段/Message 与保留 raw provider 证据；删除 media reverse bridge。首次主审指出：raw error 被 `compactError` 截断/改写且 mixed success 时丢失、failure target 错用 i18n key、`need_login` 漏计、warning 阈值被擅改、`HTTPStatus`/`RawDiagnostic` 无消费，以及 `Methodology.Parameters` 形成伪契约；同一 Worker 返工后复审通过。最终状态：Builtins 直接绑定 `mediaProbe`，producer 直接写 stable verdict/evidence/strength/table/notes/summary，typed request errors 逐请求经 `addFailure` 保留原始消息和分类，平台 ID 与展示 key 分离，七种 verdict 四桶计数一致，403 保持 unknown，HTTP 状态只保留在 machine table evidence；删除 media reverse bridge，canonical media JSON 可双语三格式重渲染且不变。主 Agent 独立证据：`go test -count=1 ./internal/i18n ./internal/probe ./internal/report`、`go test -count=1 ./...`、`go test -race -count=1 ./internal/probe ./internal/report`、`git diff --check` 均通过。
8. **route 直接机器语义 — DONE**。目标：删除 route 状态逆向扫描。输入：路由探测结果、计数和失败。产出：`routeProbe` 直接写稳定状态、摘要和展示边界机器语义。首次主审指出：首版按错误阶段指令使 `no_response` 不计入 summary，复审对照阶段前 wrapper 后恢复 `complete + no_response` 的历史 completed 语义，同时 warning 仍按 visible successes；删除未授权的 `RowIdentity` compare 变化；删除 dead route fallback keys；将 config/probe Kind 魔法字符串改为共享机器常量；补齐 mixed 双语、actual raw output 和 direct canonical shape 覆盖。返工后复审通过。最终状态：Builtins 直绑 `routeProbe`，route reverse bridge 删除；producer 直接生成 stable statuses、summary/skip reasons、fields、tables、notes、source；typed external error 保留原文，parse/tool missing 的 `Failure.Message` 不含本地化文案；默认 RouteTargets 使用 machine Kind IDs；backtrace 未改。主 Agent 独立证据：`go test -count=1 ./internal/config ./internal/i18n ./internal/probe ./internal/report`、`go test -count=1 ./...`、`go test -race -count=1 ./internal/probe ./internal/report`、`git diff --check` 均通过。真实公网 NextTrace 未在本阶段验证，确定性 fixture 已覆盖各执行状态。
9. **backtrace 直接机器语义 — DONE**。目标：删除回程状态逆向识别。输入：NextTrace raw result、hop/carrier facts。产出：`backtraceProbe` 直接生成 stable status/reason/hop/carrier/line facts 与双语可重渲染 canonical JSON，Builtins 直绑 producer，删除 backtrace reverse bridge。主审初审及后续审查由同一 Worker 返工：修复 partial usable output 被 command error 覆盖、TextBlock language 错误、长错误及 synthetic renderer 覆盖不足、direct-shape/catalog/config 约束不足；随后修复自定义 CLI 缺 carrier 导致公开用法失败（固定 `carrier:Name=host` 专用 parser 与双语 help/error）、CN2 GIA/GT inferred caveat 丢失，以及合法 all-no-response JSON 被误报为 `parse_failed`。最终状态：partial/typed error 同时保留，raw block 使用 `text` 且原文不截断；status/reason/hop/carrier/line、24 个 built-in target key、measurement/table/field/note/source/summary 全部为机器语义；GIA/GT 明确推测并有稳定 caveat；malformed/empty Hops、all-no-response、command error、partial valid 分支分别产生诚实 Failure/reason，canonical JSON 可三格式双语渲染且不变；自定义 carrier 只能为 `telecom`/`unicom`/`mobile`，旧 carrierless 语法 fail-closed。主 Agent 独立验收：`go test -count=1 ./internal/config ./internal/app ./internal/i18n ./internal/probe ./internal/report`、`go test -count=1 ./...`、`go test -race -count=1 ./internal/probe ./internal/report`、`git diff --check` 均通过；确定性 NextTrace fixture 与真实 plan resolver/CLI 路径已验证，真实公网 NextTrace 未在本阶段运行。阶段 10 已完成，未修改 route/media/network。
10. **NAT 结构化 — DONE**。目标：删除中文顿号 canonical 字段。输入：STUN server 列表。产出：删除 `stun_pool` scalar、`describeNATServers` 和本地化 join；由 `network.nat.stun_pool` table 按配置顺序逐行保存完整 Name/Address 机器事实，保留现有探测明细与状态语义。验收：成功早停、全部失败、无服务器 skip 均保留正确候选池行为；zh/en Text/Markdown/HTML 显示本地化表头和原始候选值，canonical JSON 渲染前后不变；主 Agent 独立执行 `go test -count=1 ./internal/i18n ./internal/probe ./internal/report`、`go test -count=1 ./...`、`go test -race -count=1 ./internal/probe ./internal/report`、`git diff --check` 均通过。阶段 11 已完成。
11. **system 单次采集 — DONE**。目标：删除 system 二次 collector/semantic bridge。输入：一次 `collectSystem`、一次环境快照、一次 cloud identity discovery 和一次 kernel facts 采集。产出：`systemProbe` 直接生成稳定 Result。首次主审要求返工：删除 `describeSteal`/`formatDuration`/未消费的 snapshot `MemoryLimitVia` 与测试专用资源 wrapper；扩大包级 production AST 单次调用约束；确定性 fixture 不得读取宿主 kernel；补齐 uptime NaN/Inf/uint64 溢出边界。返工后最终状态：Builtins 直绑 `systemProbe`；每次运行仅一次 system、resource、cloud、kernel 采集；uptime 在同一 snapshot 解析为 `uptime_seconds`；resource/pressure/kernel facts、stable fields/measurements/tables/notes/summary 直接构造；kernel 使用真实生产读取入口与可注入固定 facts helper；旧 bridge/dead helper 删除；zh/en Text/Markdown/HTML 渲染不泄漏 stable key 或 `%!`，canonical JSON 保持不变。主 Agent 独立验证：`gofmt -l` 无输出、`git diff --check`、`go test -count=1 ./internal/i18n ./internal/probe ./internal/report`、`go test -count=1 ./...`、`go test -race -count=1 ./internal/probe ./internal/report` 均通过；远端 target 仍为 `c6a59b37d2e61708bd3487dc13a8881fd718efaf`。未验证项：非当前 Linux 主机平台的 system collector 行为；本机 live system probe 已执行并验证 kernel stable keys/去重。
12. **摘要迁移收尾 — DONE**。目标：删除旧 Summary 双表示、renderer fallback 和临时 bridge。输入：全仓 model、renderer、failure classifier、Skip API 与 producer 写入点。产出：global `Summary` 保留 `Status`、`OK`、`Warnings`、`Skipped`、`Errors` 计数，且人类可读摘要的唯一表示为 `Messages`；result 保留 `SummaryMessages` 且删除旧字符串 `Summary`，`Skip(Message)`/`Fail(error)` 直接写结构化稳定消息，外部错误与 raw evidence 保持原样。首次主审指出摘要契约 AST 误报合法 `Report.Summary`、JSON 检查错层级、global/result legacy fallback 与 structured summary 渲染断言不足、安全测试未覆盖 Message Args，以及 Skip 语义误用和 iperf3 LookPath 原始错误丢失；已由同一 Worker 返工并经第二轮主审通过。最终状态：生产代码只保留结构化摘要字段和 Message 渲染，无 legacy fallback/兼容层；所有 Skip 调用显式传递稳定 Message，外部错误保留原文，`ecs.report/v1` 不变。主 Agent 独立验收：精确生产搜索确认无 `Summary.Headline`、legacy `Result.Summary`、`reportHeadline` 或 renderer fallback，合法 `Report.Summary` 保留；`go test -count=1 ./internal/model ./internal/report ./internal/runner ./internal/probe ./internal/app ./internal/config ./internal/compare ./internal/i18n`、`go test -count=1 ./...`、`go test -race -count=1 ./internal/model ./internal/report ./internal/runner ./internal/probe`、changed Go files `gofmt -l` 无输出、`git diff --check` 均通过；远端 target 仍为 `c6a59b37d2e61708bd3487dc13a8881fd718efaf`。
13. **schema 与任务文档 — DONE**。目标：修复契约和复审记录失真。输入：阶段 12 本地代码候选、当前 model/producer、plan/run、retry/NAT model 和验证记录。产出：`docs/schema.md`、`PLAN.md`、`REVIEW.md`、`VALIDATION.md` 更新，明确 remote target 与 local candidate，并区分已验证证据和待阶段 14/15 项。四轮主审及同一 Worker 返工依次修正：v1 原地更新纪律、stable key/Message/summary 计数区分、retry 阈值/measurement/evidence/status/attempt 形状、disk 参数与时长、network Field、NAT protocol/缺失值、backtrace 与 retry-enabled TextBlock、execution plan 的必需 `staging`，并重做 JSON 解析与工具链记录。最终独立验收确认四文件范围、`git diff --check`、11 个 JSON block 解析、与 model types/pressure/FinalizeBenchmarkRetry/network/NAT producer/executionPlan/LoadJSON 对照，以及 PR/remote 状态核验；阶段 13 收口时阶段 14/15 尚未执行，后续状态由对应阶段记录维护。验收：不再把历史 SHA 当当前状态，不再声称远端已同步或最终 CI/合并完成；`ecs.report/v1`、`ecs.plan/v1`、Message/retry/NAT 实际形状与代码一致；文档 diff 仅限四个允许文件并通过 `git diff --check`。
14. **完整本地回归 — DONE**。目标：验证八项修复及回归风险。输入：阶段 13 文档收口后的本地候选 `98f568a8ba0637a0b7553e934cb3a89af6e62311`，以及返工后仅限 network 的候选。首次回归发现 `internal/probe/network.go:172` 的 staticcheck SA4006；同一 Worker 删除未使用 `reason`，保留 `address.Err` 的 typed Failure，并新增 egress stable-field/raw-error 聚焦测试。主 Agent 完整复审确认：默认 Go1.22.2 与 Go1.26.5 focused/full/race、Go1.26.5 `scripts/ci/check.sh`、cross 七架构、submission、integration、真实二进制双语、canonical hash 不变、8 组 exposure×reveal plan（16 个文件）和 `run_test.sh` 均通过；gofmt、`git diff --check`、schema JSON blocks 11/11、临时目录清理及五文件范围均通过。Go1.26.5 本地 govulncheck 仍 exit 3，恰有 5 个可达标准库漏洞且均 fixed in Go1.26.6；按 REQUIREMENTS 不升级工具链，不能称 security clean。GitHub security、最终 PR/远端同步、发布仍未验证。阶段 15 已开始，见下一项。
15. **最终总审查与 PR 更新 — IN PROGRESS（待推送/远端 CI）**。目标：确认 base→本地候选的完整 diff 可交付，并准备更新现有 Draft PR #6。输入：base `6e85039301fc817e8c4a290ffb9b111fc37e55a8`、本地审计候选 `c6ee5ba17ca6d902f88d549b87335c67e622db7f`、远端 target `c6a59b37d2e61708bd3487dc13a8881fd718efaf`、阶段 1–14 证据和当前 PR。产出：本轮完整 diff/19 项需求/8 项 blocker 审计、PR body 草案和待主 Agent 复核的远端同步结论。当前只读核验确认 merge-base 正确、base→candidate 为 103 commits/207 files（+16252/-7115）、无二进制差异、diff-check 通过；审计开始时工作树干净，当前仅保留本阶段允许的 Markdown 修改。PR #6 仍 OPEN/Draft/CLEAN/MERGEABLE，但远端 checks 针对旧 target，不能冒充本地候选 CI。八项 blocker 在源码/测试层均已闭环；需求 14、15 的实现均满足但验证受限，外部 live/发布边界仍未验证。`SEARCH_SUMMARY.md` 已重标为原始任务历史阶段 1/2026-08-20 初始调查快照，不作为当前验收证据；第一轮因相关任务文档未重标历史状态而返工，第二轮因 `SEARCH_SUMMARY.md` 对原始任务历史与 2026-08-23 remediation 的时间线归属仍有歧义而返工，两轮返工均由同一 Worker 完成。第三轮预推送主审已通过，当前等待推送和远端 CI；未调用 `gh pr edit`，未 push。

## 当前状态与阶段记录

- **0 基线与仓库规范 — DONE**。目标：建立安全工作边界；输入：用户规范、main、README/CONTRIBUTING/CI；范围：任务文档；产出：`AGENTS.md`、`REQUIREMENTS.md`、Branch 与基准记录；验收：`AGENTS.md` 只含稳定纪律，基准固定为 `main@6e850393...`。
- **1 仓库搜索与事实固化 — DONE**。目标：把 18 项需求映射到真实实现；输入：仓库文件树/代码；范围：只读搜索；产出：`SEARCH_SUMMARY.md`；验收：已确认 app/config/model 大文件、report Localize、source-text i18n、runner retry 白名单、run.sh plan-file、leaderboard trigger 等真实现状。
- **2 `app.go` 行为保持式拆分 — DONE**。目标：命令职责显式化；输入：原 `internal/app/app.go`；范围：同一 `package app`，禁 Cobra/Command/DI；产出：run/render/list/config/doctor 等职责文件；验收：阶段 diff 基本是等量移动，未触碰其他 package。运行验证：由后续全仓 CI 一并覆盖。
- **3 唯一 `resolveRunConfig` — DONE**。目标：CLI+file+defaults 只有一个 resolver；输入：阶段 2；范围：run 配置解析；产出：`resolveRunConfig`，run 仅负责编排；验收：主审发现并返工 `--version` 早退顺序和 flag stderr 重复输出两项行为差异。运行验证：由后续全仓 CI 一并覆盖。
- **4 Compare JSON-only 契约 — DONE**。目标：输入固定 ECS JSON，`--format` 只表示 comparison output formats；输入：compare app/loader/tests；范围：help/命名/契约测试；产出：`outputFormats` 命名、JSON-only 测试；验收：MD/HTML 输入失败，合法 ECS JSON 不要求 `.json` 扩展，不存在 autodetect/parser。该阶段历史验证记录：当时 `internal/compare` 已通过，而完整 required 曾被阶段 8 中间态阻塞；后续阶段 14 已完成全仓 required 回归。
- **5 Structured comparison Notice — DONE（按新 beta 版本纪律修订）**。目标：删除 key+args 字符串编码/`ParseNotice`；输入：compare model/build/report；范围：comparison notice；产出：`Notice{Key,Args}`、renderer 直接渲染；**schema 版本保持 `ecs.compare/v1`，结构直接原地更新，不升 v2、不保留旧兼容层**；验收：结构化实现保留，只撤销此前自行升版决定，文档与测试同步保持 v1。
- **6 i18n 单向 lookup + parity — DONE**。目标：zh/en 并列 catalog；输入：i18n maps/tests；范围：静态 key lookup；产出：英文缺 key 返回 key，不回退中文；compare key 纳入 parity；验收：`lookup` 以 catalog membership 判断存在性，旧 fallback 测试反转。旧 probe source-text 层明确暂留到阶段 9。
- **7 `model.Message` 基础设施 — DONE**。目标：动态 ECS 文本具有语言无关机器语义；输入：model summary/fail、report Localize；范围：先迁 model 自生成 summary/failure，不迁 probe；产出：`Message{Key,Args}`、model message catalog、report `renderMessage`；`model.go` 不再 import i18n；验收：Message JSON round-trip、RedactedCopy 深拷贝及 Args IP 遮盖测试已写，阶段 diff 小而集中。
- **8 Runner 动态文本迁移 — DONE**。目标：runner 自生成 notices、skip/panic 等不再写 canonical Chinese；输入：阶段 7 Message；范围：runner→model 及展示边界；产出：runner notices/skip/panic 使用稳定 `Message{Key,Args}`，外部 panic/error/raw evidence 保持原样，进度标题通过 `TitleKey` 在 app/CLI 展示边界本地化，report markdown/html/txt 直接渲染 structured notices；验收：runner 无 i18n import 或源文本翻译，`PrivacyNoticeKey` 来自 ModuleDescriptor，`ecs.report/v1` 保持不变，相关 tests 已改为 key/args 契约。主审两次发现整文件替换夹带无关删除（`text.go`、`app/run.go`）并立即回退后以最小 diff 重做。真实 CI：PR run #349 的 unit、quality、integration、race、cross（7 架构）、submissions、Go 1.22 compat、required 全部 success。
- **9 Probe 动态文本迁移与删除 source-text translation — DONE**。目标：probe ECS 自生成文案改 stable key/Message；输入：`probe_text.go`、probe results；范围：不改第三方 stdout/stderr/raw evidence；产出：所有 Builtins 的 ECS-owned 标题、方法学、字段、表格、摘要、说明和动态状态使用 stable key/Message；删除 exact/regex/template source-text translation；验收：无 `i18n.Text` 反向识别入口，built-in title/descriptor 回归测试通过，关键 probe 双语从同一 JSON 渲染。
- **10 删除 report Localize clone — DONE**。目标：终结 canonical Chinese→localized copy；输入：阶段 7–9；范围：report localization；产出：renderer 直接读取 machine fields/message，在展示边界按 stable key 查当前语言；删除 `probe_text.go`、`report.Localize` 及旧 source-text catalogs；验收：同一 JSON 可 `render --lang zh|en`，不存在 `report.Localize`/反向文本翻译入口。
- **11 Report render-all-before-write — DONE**。目标：多格式输出原子成组；输入：writer；范围：render/write 顺序；产出：主报告和 comparison 均先生成全部格式 bytes 后写文件；验收：任一 renderer/未知格式失败时不留下部分文件，JSON 保持 canonical。
- **12 `ecs plan --json` — DONE**。目标：Go 成为 run planning 唯一控制面；输入：`resolveRunConfig` + ModuleDescriptor；范围：app plan/schema/tests/docs；产出：`ecs.plan/v1`、modules、required tools、外联语义、Ookla/staging/corpus 机器计划；验收：plan 复用 `resolveRunConfig` 和同一 descriptor 选择顺序，JSON 不含本地化散文，resolver/staging 回归测试通过。
- **13 `run.sh` 消除第二套 planner — DONE**。目标：Shell 仅 bootstrap/checksum/plan consumer/tool staging/run；输入：阶段 12；范围：`run.sh`；产出：删除 shell 对 profile/only/skip/config/exposure/module manifest 的业务解释，保留 staging/Ookla；验收：`sh -n run.sh` 与 `scripts/run_test.sh` 通过，fixture 消费 `ecs.plan/v1`。
- **14 Retry policy 收回 ModuleDescriptor — DONE**。目标：删除 runner 模块 ID 白名单；输入：descriptor+runner；范围：最小 metadata；产出：`RetryOnInterference bool` 并写入 `ecs.plan/v1`；验收：cpu/zstd/npb/memory/crypto/disk 集合行为不变，任意 runner ID 由 descriptor policy 决定，config/runner/plan 回归测试通过。
- **15 拆分 `config.go` — DONE**。目标：职责稳定后物理模块化；输入：阶段 12/14 后 config；范围：同 package；产出：`types.go`、`defaults.go`、`file.go`、`validate.go`、`catalog.go`、`network.go` 与既有 endpoints/exposure/modules 边界；验收：`internal/config`、`app`、`probe`、`runner` 测试通过，ModuleDescriptor 仍为模块元数据唯一事实源。
- **16 拆分 `model.go` — DONE**。目标：Message 模型稳定后物理拆分；输入：阶段 7–10；范围：同 package；产出：`types.go`、`result.go`、`message.go`、`evidence.go`、`failure.go`、`summary.go`、`redact.go`、`format.go`；验收：model 无 i18n，`model`、`report`、`compare`、`probe`、`runner`、`app` 测试通过，JSON/脱敏行为保持。
- **17 `tools/lock.json` — DONE**。目标：统一内部供应链机器事实；输入：common/build/release 固定值；范围：内部构建链；产出：`tools/lock.json`（架构、工具 tag/commit/source、STREAM、NPB、语料摘要/尺寸/拼接顺序）；`common.sh`、工具构建、语料构建和发布校验从锁读取；验收：`scripts/lock_test.sh`、工具 stage/package 回归与全部 shell 语法通过，`run.sh` 不读取仓库 lock。
- **18 Staticcheck cache 绑定 devtools lock — DONE**。目标：go.mod/go.sum 变化自动重建；输入：check/devtools；范围：缓存判定；产出：`.devtools-bin/staticcheck.lock`（由 `devtools/go.mod` + `go.sum` 摘要派生）；验收：`scripts/devtools_cache_test.sh` 证明匹配可复用、lock 变化失效，质量检查接入该门禁。
- **19 拆 `build_tools.sh` — DONE**。目标：按工具拆 shell，不造框架；输入：阶段 17；范围：`scripts/tools/*.sh` + 顶层编排；产出：sysbench/zstd/NPB/OpenSSL/STREAM/fio/iperf3/NextTrace/ping 的独立构建与 smoke 函数，顶层仅保留取源、静态校验、许可和 manifest 编排；验收：无 ToolBuilder/provider/plugin，架构/产物布局保持，全部新增脚本通过 `bash -n`。
- **20 CI/Security 边界 — DONE**。目标：quality 语义准确，security 独立；输入：workflows/check；范围：CI；产出：quality 注释仅描述静态/格式/契约检查，新增独立 `security.yml`，按周或手动运行固定版本 `govulncheck`；验收：未接入 `ci.required`，仍读取人工固定的 `.go-version`，未引入自动升级。
- **21 Leaderboard trigger — DONE**。目标：消除写回 baseline 自触发并覆盖真实算法入口；输入：workflow/score；范围：paths；产出：排除 `submissions/baseline.json` 与 `internal/score/embedded/baseline.json` 两个生成输出，同时显式监听 leaderboard 脚本、baseline/score/submission/tier 算法、模块评分元数据和模型入口；验收：输出文件不自触发，提交目录与真实算法入口仍触发。
- **22 Installer/docs parity — DONE**。目标：保留显式 `--with-benchmarks` 并修陈旧文案；输入：install/README/README_EN/docs；范围：脚本与文档；产出：安装器默认使用 `CST-Cat/ecs`，镜像/派生仓库仍可用 `ECS_REPOSITORY` 覆盖，中英文安装说明与 examples 同步；验收：persistent install 仍仅在显式 `--with-benchmarks` 时安装 sysbench/fio/iperf3，`sh -n install.sh` 与安装行为回归通过。
- **23 Probe 小范围去重 — DONE**。目标：只抽真实重复；输入：route/backtrace/external commands；范围：NextTrace adapter/有限 command helper；产出：保留 route/backtrace 共用的 NextTrace adapter，并将多个探针实际共用的版本读取、binary SHA-256、ANSI/NUL 输出清洗和错误尾部截断集中到 `tool_execution.go`；验收：无 GenericBenchmarkRunner/provider/plugin，`go test ./internal/probe` 通过。
- **24 完整 Branch diff 与总审查 — 历史记录（本轮已重新打开）**。目标：全任务回归；输入：历史 Branch vs baseline、CI/docs；范围：全仓；产出：历史 `REVIEW.md`、`VALIDATION.md` 和阶段记录；说明：本轮复审发现阻断问题，历史结论不作为当前验收。
- **25 分支交付 — 历史记录（本轮尚未交付）**。目标：历史提交 `a938385` 曾推送到 tracking 分支；说明：本轮交付目标已改为修复后更新 Draft PR #6，不能使用该历史交付记录宣称当前完成。

## 计划调整记录

- 早期计划曾记录直接推送及 Pull Request 策略；该结论仅属于历史阶段记录。本轮以现有 Draft PR #6 为交付对象，最终远端同步与 PR 更新由阶段 15 决定。
- 复审目标从历史交付状态重新打开 8 项问题；remediation 阶段 1–15 的状态以本文件“Remediation 阶段记录”为准，历史 0–25 的 DONE 标记不构成本轮通过证据。
- 阶段 5 最初因 structured notices 改变 JSON 字段类型而自行将 comparison schema 升到 `ecs.compare/v2`。用户随后明确 beta 阶段不以兼容性驱动版本升级；**结构化 Notice 设计不回滚，只把版本标识恢复并固定为 `ecs.compare/v1`，直接原地更新 v1 契约。**
- 同一规则适用于阶段 8 的主报告 Message/notices 变化：保留新结构，`ecs.report/v1` 不升 v2，不为旧结构保留兼容 schema。
- 阶段 7 采用渐进迁移：新增 `Summary.Messages` / `Result.SummaryMessages`，旧 presentation string 暂作任务内迁移字段；完成结构化迁移后阶段 10 直接删除 Localize/旧 presentation 路线，不以兼容性为理由长期保留。
